  <h1><%= t('.クラス名') %></h1>
  <p><%= notice %></p>
  <table>
    <thead>
      <tr>
        <th><%= t('.クラス名') %></th>
        <th colspan="3"></th>
      </tr>
    </thead>

    <tbody>
      <% @teams.each do |team| %>
        <%# <% if team.user.families.first.id == current_user.families.first.id %>
        <tr>
          <td><%= team.name %></td>
          <td><%= link_to t('.show'), team_path(team) %></td>
          <td><%= link_to t('.edit'), edit_team_path(team) %></td>
          <td><%= link_to t('.delete'), team, method: :delete, data: { confirm: 'Are you sure?' } %></td>
        </tr>
        <%# <% end %>
      <% end %>
    </tbody>
  </table>

  <br>

  <%= link_to t('.register_team'), new_team_path, class: "login" %>
  <%# <%= link_to t('.出席簿'), attendancebooks_path, class: "login" %>
</div>